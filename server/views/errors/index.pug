extends ../layouts/application.pug

block content
  h1 Oh no, something went wrong
  p(style="white-space: pre-wrap;")
    img(src="/assets/Sadge.png" alt="sadge")
    <b>#{error.status}</b>: #{error.message}